import 'package:flutter/cupertino.dart';

class CheckBoxWidgetStyle {
  BorderSide? side;
  Color? activeColor;
  Color? checkColor;
  CheckBoxWidgetStyle({
    this.side,
    this.activeColor,
    this.checkColor,
  });
}