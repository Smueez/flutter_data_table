import 'package:flutter/cupertino.dart';

class CheckBoxWidgetModel {
  BorderSide? side;
  Color? activeColor;
  Color? checkColor;
  CheckBoxWidgetModel({
    this.side,
    this.activeColor,
    this.checkColor,
  });
}